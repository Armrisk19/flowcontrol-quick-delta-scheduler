# FLOWCONTROL Five-Minute Quick Delta Scheduler v1.1.0

Small public scheduler repository for Gateway v8.3.0. It refreshes public market-ranking context only and contains no account, order, position, wallet, subscriber, or private strategy source data.

Add Actions secrets:
- FLOWCONTROL_GATEWAY_URL
- FLOWCONTROL_GATEWAY_TOKEN

Run the workflow once manually after deployment.
